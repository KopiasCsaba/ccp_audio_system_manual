var regexpString = script.addStringParameter("Regexp", "The regexp to match", 2, 0, 10);

function filter(inputs, minValues, maxValues, multiplexIndex) {

    return inputs;
        // // Create a RegExp object from the provided pattern
        // var regExp = new RegExp(regexpString);
        //
        // script.log(regExp);
        // return inputs;
        //
        // var result = [];
        // for (var i = 0; i < inputs.length; i++) {
        //     // Check if the input matches the regular expression
        //     result[i] = regExp.test(inputs[i]); // Returns true if it matches, false otherwise
        // }
        //
        // return result; // Return an array of boolean values

}
